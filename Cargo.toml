[package]
name = "blinkr"
version = "0.1.0"
authors = ["Peatral <contact@peatral.xyz>"]
edition = "2018"

[lib]
crate-type = ["staticlib"]

[dependencies]
pebble-rust = {path = "../pebble-rust"}

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

